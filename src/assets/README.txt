PUBLIC FOLDER
=============
Files placed in this folder are served from the root of your website.

Add these files here before deploying:
- favicon.ico  (browser tab icon — make from your logo)
- robots.txt   (already created — tells search engines what to crawl)
- sitemap.xml  (already created — lists your URLs for Google)
